<resources>

    <!-- Preference style -->
    <style name="PreferenceStyle">
        <item name="android:layout">@layout/preference_material</item>
    </style>

    <!-- SwitchPreference style -->
    <style name="SwitchPreferenceCompatStyle" parent="@style/Preference.SwitchPreferenceCompat">
        <item name="android:fontFamily">@string/fcm_fallback_notification_channel_label</item>
        <!-- Add more custom attributes here if needed -->
    </style>

    <style name="Preference.SwitchTextAppearance">
        <item name="android:textColor">@color/colorAccent</item>
    </style>

</resources>
